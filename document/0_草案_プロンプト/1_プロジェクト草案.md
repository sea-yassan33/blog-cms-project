# 概要

- プロジェクトの草案を作成してもらう

## 【USER】

```txt
Next.jsのプロジェクトからpayloadを入れる方法を簡単なブログ作成のCMSを作成したいです。
導入手順と作成手順を作成してください。
payloadの導入は「npm create payload-app@latest」からお願いします
技術は下記を使用してください。
・Next.js（最新）
・TypeScript 
・TailwindCSS 
・shadcn/ui 
・payload(template:blank)
・PostgreSQL

機能は下記の実装をしたいです。
・管理画面で記事の作成
・フロント画面にて表示されている記事に「イイね」ボタンの実装
・フロント画面にて表示されている記事にコメントする機能の実装
・「イイね」ボタンやコメントする機能は管理画面にて登録したユーザのみ大丈夫です

以上の要件でmarkdown形式で手順書を作成してください。
```

## 【AI：出力】

Markdownファイルとして手順書を作成します。Next.js + Payload CMS ブログの手順書を作成しました。手順書の構成は以下の通りです。

**全9ステップで構成：**

1. **環境準備** — Node.js / pnpm / PostgreSQL の前提条件
2. **Payload アプリ作成** — `pnpm create payload-app@latest` で blank テンプレートを使って生成
3. **PostgreSQL 設定** — DB作成と `.env` の設定
4. **プロジェクト初期設定** — TailwindCSS の導入とディレクトリ構成
5. **shadcn/ui 導入** — Button / Card / Textarea / Avatar など必要コンポーネントを追加
6. **Payload コレクション作成** — Users（認証付き）/ Posts / Media / Likes / Comments の5コレクション
7. **フロントエンド実装** — 記事一覧・詳細ページ
8. **イイね・コメント機能** — API Route + クライアントコンポーネント
9. **動作確認** — 管理者作成から機能テストまで

**認証の仕組み：** 管理画面（`/admin`）でログインすると Payload が Cookie（`payload-token`）を発行します。フロント画面の API はこの Cookie を検証して認証済みかどうかを判定するため、管理画面でのログインだけでイイね・コメントが使えます。

- [出力ファイル](./生成されたファイル/nextjs-payload-blog-guide.md)