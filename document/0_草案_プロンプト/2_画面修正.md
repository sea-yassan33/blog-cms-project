## 【USER】

あなたは●●●●●●について知っていますか？●●●●●●風のタイムラインになる様に下記のコードを修正したいのです。

```typescriptreact
import { getPayload } from 'payload'
import config from '@payload-config'
import Link from 'next/link'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

export const revalidate = 60 // 60秒ごとに再生成

export default async function HomePage() {
  const payload = await getPayload({ config })

  const posts = await payload.find({
    collection: 'posts',
    where: {
      status: { equals: 'published' },
    },
    sort: '-publishedAt',
    depth: 1,
  })

  return (
    <div>
      <h2 className="text-xl font-semibold mb-6 text-gray-700">記事一覧</h2>
      {posts.docs.length === 0 ? (
        <p className="text-gray-500">まだ記事がありません。</p>
      ) : (
        <div className="grid gap-6">
          {posts.docs.map((post) => (
            <Link key={post.id} href={`/${post.slug}`}>
              <Card className="hover:shadow-md transition-shadow cursor-pointer">
                <CardHeader>
                  <div className="flex items-center justify-between">
                    <CardTitle className="text-lg">{post.title}</CardTitle>
                    <Badge variant="secondary">
                      {post.publishedAt
                        ? new Date(post.publishedAt).toLocaleDateString('ja-JP')
                        : ''}
                    </Badge>
                  </div>
                </CardHeader>
                {post.excerpt && (
                  <CardContent>
                    <p className="text-gray-600 text-sm">{post.excerpt}</p>
                  </CardContent>
                )}
              </Card>
            </Link>
          ))}
        </div>
      )}
    </div>
  )
}

```

payloadのpostsコレクションの内容は下記の通りになります。

```typescript
import type { CollectionConfig } from 'payload'

export const Posts: CollectionConfig = {
  slug: 'posts',
  admin: {
    useAsTitle: 'title',
    defaultColumns: ['title', 'status', 'publishedAt', 'updatedAt'],
  },
  access: {
    // 公開記事は誰でも読める
    read: ({ req }) => {
      if (req.user) return true
      return {
        status: {
          equals: 'published',
        },
      }
    },
    // 作成・更新・削除は認証済みユーザーのみ
    create: ({ req }) => !!req.user,
    update: ({ req }) => !!req.user,
    delete: ({ req }) => !!req.user,
  },
  fields: [
    {
      name: 'title',
      type: 'text',
      required: true,
      label: 'タイトル',
    },
    {
      name: 'aiGenerate',
      type: 'ui',
      admin: {
        components: {
          Field: '@/components/admin/AIGenerateButton#AIGenerateButton',
        },
      },
    },
    {
      name: 'slug',
      type: 'text',
      required: true,
      unique: true,
      label: 'スラッグ（URL）',
      admin: {
        description: '例: my-first-post',
      },
    },
    {
      name: 'excerpt',
      type: 'textarea',
      label: '概要',
    },
    {
      name: 'content',
      type: 'richText',
      label: '本文',
      required: true,
    },
    {
      name: 'thumbnail',
      type: 'upload',
      relationTo: 'media',
      label: 'サムネイル',
    },
    {
      name: 'status',
      type: 'select',
      options: [
        { label: '下書き', value: 'draft' },
        { label: '公開', value: 'published' },
      ],
      defaultValue: 'draft',
      required: true,
      label: 'ステータス',
    },
    {
      name: 'publishedAt',
      type: 'date',
      label: '公開日時',
      admin: {
        date: {
          pickerAppearance: 'dayAndTime',
        },
      },
    },
    {
      name: 'author',
      type: 'relationship',
      relationTo: 'users',
      label: '著者',
    },
  ],
}
```

これらの条件で●●●●●●風のタイムラインにフロント画面を作成してくれませんか？

## 【出力内容】

●●●●●●は知っています！プロジェクト管理ツールで、課題一覧がリスト形式でコンパクトに並び、ステータスや日付がバッジで表示されるUIが特徴的ですね。

まずデザインスキルを確認してから実装します。●●●●●●のデザインを忠実に再現しつつ、プロダクション品質で実装します。実装した●●●●●●風タイムラインの主なポイントをまとめます。

**レイアウト構成**
- 左側に紺色のサイドバー（●●●●●●のプロジェクトナビ風）
- 上部に固定トップバー（ページタイトル＋新規追加ボタン）
- メインエリアにタイムライン表示

**タイムライン表示**
- 記事を月ごとにグループ化し、月ヘッダー（📅 2025年1月）で区切る
- 各月の記事はカード内にリスト形式で並べ、左端に縦線＋青ドットのタイムライン軸を配置
- 各行に「日付 / 公開バッジ / タイトル / 概要 / 著者アバター」を表示

**●●●●●●らしさを出した部分**
- 行ホバーで薄青ハイライト
- タイトルはリンク色（青）でアンダーライン
- フィルターチップバー（すべて / 今月 / 先月 / 今年）
- コンパクトな情報密度（フォントサイズ13px基調）

既存コードへの変更点は `HomePage` 関数の return 部分だけなので、`payload.find()` 以上は変更不要です。`formatDate` / `getMonthLabel` / `groupByMonth` のヘルパー関数を追加した形になっています。

- [出力ファイル](./生成されたファイル/2_フロント画面.tsx)


## 【USER】

スタイルですが、可能な限りtailwindCssで実装してほしいです。難しい場合は独自のCSSを当ててよいです。

## 【出力内容】

わかりました！TailwindCSSを最大限活用して書き直します。TailwindCSS版に全面書き換えました。独自CSSは一切なくなっています。

**変更のポイント**

前回の `<style>` ブロックをすべて削除し、Tailwindクラスに置き換えました。Tailwindの標準クラスで表現できないものは任意値（`w-[220px]`、`text-[13px]`、`bg-[#1f76c8]` など）を使って忠実に再現しています。

具体的に置き換えた主なスタイルはこちらです。

- `position: fixed` → `fixed top-0 left-0 bottom-0`
- `border-left: 3px solid` → `border-l-[3px] border-[#1f76c8]`
- `ring` を使ったドットの二重リング → `border-2 border-white ring-[1.5px] ring-[#1f76c8]`
- `-webkit-line-clamp` → `line-clamp-1`（Tailwind v3.3+対応）
- ホバー時のグループ連動スタイル → `group` / `group-hover:underline` / `group-hover:bg-[#1560a8]`

- [出力内容](./生成されたファイル/2_フロント画面_tailwindcss.tsx)